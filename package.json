{
  "name": "youth-sports-hub",
  "version": "1.0.0",
  "description": "Youth Sports Association Website - Project 2",
  "scripts": {
    "build": "mkdir -p dist/api && cp index.html dist/ && cp api/*.json dist/api/",
    "start": "npx http-server dist"
  }
}
